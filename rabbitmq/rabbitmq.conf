# Carga la topología (exchanges/colas/bindings/DLQ) al arrancar el nodo.
# Importación idempotente (merge): no elimina objetos existentes.
definitions.import_backend = local_filesystem
definitions.local.path = /etc/rabbitmq/definitions.json
